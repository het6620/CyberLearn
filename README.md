# CyberLearn — Daily Vulnerability Trainer (PostgreSQL)
30-day advanced web exploitation track (24 Jun -> 24 Jul 2026).
## Local: cp .env.example .env, set DATABASE_URL, npm install, npm start
## Render: push to GitHub -> New -> Blueprint -> select repo (render.yaml auto-creates DB)
